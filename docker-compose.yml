version: '3.8'

services:
  pm2:
    build: .
    container_name: pm2-rust
    volumes:
      - pm2-data:/root/.pm2
      - ./examples:/app/examples
    working_dir: /app
    command: pm2 start examples/node-server/app.js --name node-server
    restart: unless-stopped

volumes:
  pm2-data:
